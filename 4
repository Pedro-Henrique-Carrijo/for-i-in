#Escreva um algoritmo que leia 10 números do usuário e calcule a soma desses  números.  
soma = 0
for _ in range(10):
    n = float(input("Qual o numero ?"))
    soma += n
    print("A soma atual dos seus numeros é", soma)
