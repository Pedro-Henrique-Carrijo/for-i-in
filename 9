#  Escreva um algoritmo que leia 20 números do usuário e exiba quantos números são  pares  
pares = 0
for _ in range(20):
    numeros = float(input("Qual sua idade?"))
    if numeros % 2 == 0:
        pares += 1
print(f"no total {pares} numeros são pares")
